{% set client_type = process.processClient.type|default(null) %}
{% if client_type in ['company', 'person'] %}
    {% include 'process/_sidebar_process_info/client/' ~ client_type ~ '.html.twig' with {client: process.processClient} only %}
{% endif %}
