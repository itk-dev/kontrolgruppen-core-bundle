{% extends 'base.html.twig' %}

{% block title %}RevenueEntry{% endblock %}

{% block body %}
    <h1>RevenueEntry</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ revenue_entry.id }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ revenue_entry.amount }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ revenue_entry.type }}</td>
            </tr>
            <tr>
                <th>FutureSavingsType</th>
                <td>{{ revenue_entry.futureSavingsType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('revenue_entry_index') }}">back to list</a>

    <a href="{{ path('revenue_entry_edit', {'id': revenue_entry.id}) }}">edit</a>

    {{ include('revenue_entry/_delete_form.html.twig') }}
{% endblock %}
