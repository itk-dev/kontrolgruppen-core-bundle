{% extends '@KontrolgruppenCore/process/main.html.twig' %}

{% block title %}{{ 'economy.revenue.title'|trans }}{% endblock %}

{% block content %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

    {{ dump() }}

    {% if revenue is defined %}
        {% for repaymentEntry in revenue.repaymentEntries %}
            {{ repaymentEntry.id }}
        {% endfor %}
    {% endif %}
{% endblock %}

{% block javascripts %}
{% endblock %}
