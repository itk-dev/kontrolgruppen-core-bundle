{% extends '@KontrolgruppenCore/main/index.html.twig' %}

{% block title %}{{ 'channel.show.title'|trans }}{% endblock %}

{% block content %}
    <h1>{{ 'channel.show.title'|trans }}</h1>

    <div class="table-responsive">
        <table class="table table-striped table-hover table-borderless thead-light">
            <tbody>
            <tr>
                <th>{{ 'channel.table.name'|trans }}</th>
                <td>{{ channel.name }}</td>
            </tr>
            </tbody>
        </table>
    </div>

    <a href="{{ path('channel_index') }}" class="btn btn-light float-left">{{ 'common.back_to_list'|trans }}</a>

    <a href="{{ path('channel_edit', {'id': channel.id}) }}" class="btn btn-primary float-right pr-5 pl-5">{{ 'common.actions.edit'|trans }}</a>

    {{ include('@KontrolgruppenCore/channel/_delete_form.html.twig') }}
{% endblock %}
