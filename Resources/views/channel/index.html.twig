{% extends '@KontrolgruppenCore/main/index.html.twig' %}

{% block title %}{{ 'channel.index.title'|trans }}{% endblock %}

{% block content %}
    <h1 class="float-left">{{ 'channel.index.title'|trans }}</h1>
    <a href="{{ path('channel_new') }}" class="btn btn-success float-right btn-lg">{{ 'channel.btns.create_new'|trans }}</a>
    <div class="table-responsive">
        <table class="table table-striped table-hover table-borderless thead-light">
            <thead>
            <tr>
                <th>{{ 'channel.table.name'|trans }}</th>
                <th class="float-right">{{ 'common.table.actions'|trans }}</th>
            </tr>
            </thead>
            <tbody>
            {% for channel in channels %}
                <tr>
                    <td>{{ channel.name }}</td>
                    <td class="text-right">
                        <a href="{{ path('channel_show', {'id': channel.id}) }}" class="btn btn-sm btn-dark" data-toggle="tooltip" data-placement="top"
                           title="{{ 'common.actions.show'|trans }}">
                            <i class="fas {{ iconClass('show') }} fa-inverse"></i>
                        </a>
                        <a href="{{ path('channel_edit', {'id': channel.id}) }}" class="btn btn-sm btn-primary" data-toggle="tooltip" data-placement="top"
                           title="{{ 'common.actions.edit'|trans }}">
                            <i class="fas {{ iconClass('edit') }} fa-inverse"></i></a>
                        </a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="3">{{ 'common.no_records_found'|trans }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
