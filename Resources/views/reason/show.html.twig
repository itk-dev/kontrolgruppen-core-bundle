{% extends '@KontrolgruppenCore/main/index.html.twig' %}

{% block title %}{{ 'reason.show.title'|trans }}{% endblock %}

{% block content %}
    <h1>{{ 'reason.show.title'|trans }}</h1>

    <div class="table-responsive">
        <table class="table table-striped table-hover table-borderless thead-light">
            <tbody>
            <tr>
                <th>{{ 'reason.table.name'|trans }}</th>
                <td>{{ reason.name }}</td>
            </tr>
            </tbody>
        </table>
    </div>

    <a href="{{ path('reason_index') }}" class="btn btn-light float-left">{{ 'common.back_to_list'|trans }}</a>

    <a href="{{ path('reason_edit', {'id': reason.id}) }}" class="btn btn-primary float-right pr-5 pl-5">{{ 'common.actions.edit'|trans }}</a>

    {{ include('@KontrolgruppenCore/reason/_delete_form.html.twig') }}
{% endblock %}
