{% extends 'main/index.html.twig' %}

{% block title %}{{ 'process_status.index.title'|trans }}{% endblock %}

{% block content %}
    <h1>{{ 'process_status.index.title'|trans }}</h1>

    <table class="table">
        <thead>
            <tr>
                <th>{{ 'process_status.table.id'|trans }}</th>
                <th>{{ 'process_status.table.name'|trans }}</th>
                <th>{{ 'common.table.actions'|trans }}</th>
            </tr>
        </thead>
        <tbody>
        {% for process_status in process_statuses %}
            <tr>
                <td>{{ process_status.id }}</td>
                <td>{{ process_status.name }}</td>
                <td>
                    <a href="{{ path('process_status_show', {'id': process_status.id}) }}">{{ 'common.actions.show'|trans }}</a>
                    <a href="{{ path('process_status_edit', {'id': process_status.id}) }}">{{ 'common.actions.edit'|trans }}</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">{{ 'common.no_records_found'|trans }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('process_status_new') }}">{{ 'common.actions.create'|trans }}</a>
{% endblock %}
