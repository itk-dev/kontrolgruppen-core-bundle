{% extends 'main/index.html.twig' %}

{% block title %}{{ 'journal_entry.edit.title'|trans }}{% endblock %}

{% block content %}
    <h1>{{ 'journal_entry.edit.title'|trans }}</h1>

    {{ include('journal_entry/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('journal_entry_index', {'process': process.id}) }}">{{ 'common.back_to_list'|trans }}</a>

    {{ include('journal_entry/_delete_form.html.twig') }}
{% endblock %}
