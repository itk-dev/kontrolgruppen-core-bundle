{% extends "@FOSUser/layout.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}
{% block page_title 'security.login.submit' | trans %}

{% block fos_user_content %}
<form action="{{ path("fos_user_security_check") }}" method="post" class="form-signin">
  <h1 class="h3 mb-3 font-weight-normal">{{ 'security.login.title'|trans }}</h1>
  {% if error %}
    <div class="has-error">
      <p class="error-block">
        <span class="label label-danger">
          {{ 'errors'|transchoice(1, domain = 'EasyAdminBundle') }}
        </span>
        {{ error.messageKey|trans(error.messageData, 'security') }}
      </p>
    </div>
  {% endif %}

  <input type="hidden" name="_csrf_token" value="{{ csrf_token }}"/>

  <div class="form-group">
    <label for="username" class="control-label">{{ 'security.login.username'|trans }}</label>
    <input class="form-control" type="text" id="username" name="_username" value="" placeholder="{{ 'security.login.username'|trans }}" required="required"/>
  </div>

  <div class="form-group">
    <label for="password" class="control-label">{{ 'security.login.password'|trans }}</label>
    <input class="form-control" type="password" id="password" name="_password" placeholder="{{ 'security.login.password'|trans }}" required="required" value=""/>
  </div>
{#
  <div class="form-group">
    <a href="{{ path('fos_user_resetting_request') }}">{{ 'resetting.reset.forgot'|trans }}</a>
  </div>
#}
  <div class="form-group">
    <button type="submit" class="btn btn-lg btn-primary btn-block" id="_submit" name="_submit">{{ 'security.login.submit'|trans }}</button>
  </div>
</form>
{% endblock fos_user_content %}
