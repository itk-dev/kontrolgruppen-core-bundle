{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('fos_user_change_password', {'referer': path('easyadmin')}), 'attr': { 'class': 'fos_user_change_password' } }) }}
  {{ form_widget(form) }}

  <button type="submit" class="btn btn-lg btn-primary btn-block">{{ 'change_password.submit'|trans }}</button>
{{ form_end(form) }}
