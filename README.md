# kontrolgruppen-core-bundle

Kontrolgruppen core bundle.

## Building js and css

Uses webpack to build js and css assets.

To build assets:
```
yarn run build
```

To continually build assets:
```
yarn run watch
```
